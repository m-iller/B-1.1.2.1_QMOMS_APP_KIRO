# Database
DB_USER=quarry_user
DB_PASSWORD=changeme

# JWT
JWT_SECRET=supersecretkey
JWT_EXPIRES_IN=8h

# Simulator API token (must match backend SIM_API_TOKEN)
SIM_API_TOKEN=sim_token_changeme

# Frontend
VITE_API_URL=http://localhost:8000
